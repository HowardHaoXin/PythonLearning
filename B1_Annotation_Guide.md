# Python 注释 (Annotation) 完全指南

> 对应源码：[src/B1_Annotation.py](src/B1_Annotation.py)

---

## 1. 什么是注释？

注释是写在代码中、但**不会被 Python 解释器执行**的文字。它的作用是帮助开发者（包括未来的自己）理解代码的意图、逻辑和注意事项。

运行 `src/B1_Annotation.py` 的输出结果：

```
123
123
```

可以看到，代码中的所有注释文字（以 `#` 或 `'''` 包裹的内容）都没有出现在输出中——它们只存在于源码里，仅供人阅读。

---

## 2. 单行注释

### 2.1 语法

以 `#` 开头，`#` 之后的所有内容都是注释，到行尾结束。

### 2.2 写法一：注释独占一行（推荐）

```python
# 输出123
print(123)
```

注释单独占一行，放在被说明代码的**上方**。这是最清晰、最常用的写法。

### 2.3 写法二：注释跟在代码后

```python
print(123)  # 输出123
```

注释紧跟在代码语句后面。适合简短的说明，但不建议注释过长（会超出屏幕宽度，影响阅读）。

### 2.4 适用场景

| 场景 | 示例 |
|------|------|
| 解释代码意图 | `# 计算用户年龄` |
| 标记 TODO | `# TODO: 需要处理空值情况` |
| 临时禁用代码（调试用） | `# print(debug_info)` |
| 标注注意事项 | `# 注意：此处不能使用浮点数` |

---

## 3. 多行注释

### 3.1 语法

使用**三个连续的单引号** `'''` 或**三个连续的双引号** `"""` 包裹注释内容。

### 3.2 写法一：多行块注释

```python
'''
第一种写法：
注释内容可以跨越多行
适合长段说明
'''
```

### 3.3 写法二：单行多行注释

```python
'''第二种写法：注释内容写在一行'''
```

即使只写一行，也使用三引号包裹，这样随时可以扩展为多行。

### 3.4 适用场景

| 场景 | 示例 |
|------|------|
| 文件头说明 | 描述整个 `.py` 文件的用途、作者、日期 |
| 函数/类文档字符串 | `def foo(): """这是函数的说明"""` |
| 大段逻辑解释 | 解释复杂算法的步骤 |
| 临时禁用多行代码 | 调试时把一段代码用 `'''` 包起来 |

### 3.5 关键区别

```python
# 这是单行注释，Python 完全忽略它

'''
这是多行字符串字面量。
如果它没有赋值给变量、也不是函数/类的第一条语句，
Python 同样会忽略它，效果等同于注释。
'''

def my_func():
    """这是 docstring（文档字符串），
    作为函数的第一条语句时，
    可以通过 my_func.__doc__ 访问。"""
    pass
```

---

## 4. 快速注释 / 取消注释

### 4.1 快捷键

在大多数编辑器（包括 VS Code）中：

| 操作 | 快捷键 |
|------|--------|
| 注释选中行 | `Ctrl + /` |
| 取消注释 | `Ctrl + /`（再次按下） |

### 4.2 原理

编辑器会自动在选中的每一行前面添加或移除 `#` 符号。这是日常开发中使用频率最高的快捷键之一。

---

## 5. 最佳实践

1. **注释解释"为什么"，而不是"做什么"**——代码本身已经说明了"做什么"。
   ```python
   # ❌ 无意义注释
   x = x + 1  # x 加 1

   # ✅ 有价值的注释
   x = x + 1  # 跳过标题行，从第2行开始计数
   ```

2. **保持注释与代码同步**——修改代码时，务必同步更新注释，过时的注释比没有注释更危险。

3. **优先使用英文或统一语言**——如果团队国际化，用英文；如果是个人学习项目，用中文也没问题。

4. **不要过度注释**——好的变量名和函数名本身就是最好的注释。

---

## 6. 本系列其他文件

| 文件 | 主题 |
|------|------|
| `B1_Annotation.py` | 注释 |
| `B2_FunPrint.py` | print 函数 |
| `B3_variable.py` | 变量 |
| `B4_FunInput.py` | input 输入函数 |
| `B5_Operator.py` | 运算符 |
| `B6_String.py` | 字符串 |
| `B7_List.py` | 列表 |
| `B8_Tuple.py` | 元组 |
| `B9_Dictionary.py` | 字典 |
| `B10_Set.py` | 集合 |
| `B11_DataType.py` | 数据类型 |
| `B12_ConditionalAndLoop.py` | 条件与循环 |
| `B13_def.py` | 函数定义 |
| `B14_DefApplication.py` | 函数应用 |

---

*文档生成时间：2026-06-16*
